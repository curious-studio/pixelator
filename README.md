# Pixelator

A fully client-side image pixelation tool. It uploads a source image, samples it into a fixed 20 by 20 grid, renders the result on canvas, and exports PNG.

Open `index.html` in a browser to use it.
